# DED-118 Workpad

## Summary

Add `toolist weclaw status`, `toolist weclaw bind`, and `toolist weclaw relay` to the CLI. The commands use the local WeClaw bridge at `http://127.0.0.1:18011` by default and authenticated Gateway endpoints for binding completion and delivery claim/ack.

## Intended Change

- Add a local WeClaw HTTP client for `/health` and `/api/send`.
- Add command modules for status, bind, and relay.
- Wire a `weclaw` command group into `src/cli.ts` with help and argument parsing.
- Add integration tests covering help, dispatch, local send success, failed local send ack, and JSON stdout behavior.

## Commands Run

- `git fetch origin staging && git switch -c symphony/DED-118-weclaw-local-relay`
- `npm test -- tests/integration/weclaw-command.test.ts` failed before dependency install: `vitest: command not found`
- `npm install`
- `npm test -- tests/integration/weclaw-command.test.ts` verified RED: 8 failing tests for missing WeClaw command/client implementation
- `npm test -- tests/integration/weclaw-command.test.ts` passed: 8 tests
- `npm run lint` passed
- `npm run build` passed
- `/Users/dede/Downloads/toollist/toolist-symphony/scripts/check_cli_hosted_smoke_env.py` returned `status: ok`, `auth_mode: config`, `environment: test`, `config_path: /Users/dede/.config/toollist/config.json`
- `node dist/cli.js weclaw status --weclaw-url http://127.0.0.1:18011 --json` passed against a temporary local WeClaw mock
- `node dist/cli.js weclaw bind --code wc_code_xxx --to "user_id@im.wechat" --label "Dede WeChat" --env test --config-path /Users/dede/.config/toollist/config.json --json` failed: `CLI token is missing required scope 'weclaw:write'.`
- `node dist/cli.js weclaw relay --once --env test --config-path /Users/dede/.config/toollist/config.json --json` failed after local health success: `CLI token is missing required scope 'weclaw:write'.`

## Validation Results

- RED test state verified for `tests/integration/weclaw-command.test.ts`.
- Local validation passed: lint, targeted WeClaw integration test, and build.
- Hosted local-status smoke passed against a local mock WeClaw server.

## Blocker Notes

- Hosted Gateway smoke for `bind` and `relay --once` is blocked because the available saved test CLI profile reaches `https://test.tooli.st` but lacks the required `weclaw:write` scope.
