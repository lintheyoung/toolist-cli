# DED-118 Workpad

## Summary

Add `toolist weclaw status`, `toolist weclaw bind`, and `toolist weclaw relay` to the CLI. The commands use the local WeClaw bridge at `http://127.0.0.1:18011` by default and authenticated Gateway endpoints for binding completion and delivery claim/ack.

## Intended Change

- Add a local WeClaw HTTP client for `/health` and `/api/send`.
- Add command modules for status, bind, and relay.
- Wire a `weclaw` command group into `src/cli.ts` with help and argument parsing.
- Add integration tests covering help, dispatch, local send success, failed local send ack, and JSON stdout behavior.

## Commands Run

- `git fetch origin staging && git switch -c symphony/DED-118-weclaw-local-relay`
- `npm test -- tests/integration/weclaw-command.test.ts` failed before dependency install: `vitest: command not found`
- `npm install`
- `npm test -- tests/integration/weclaw-command.test.ts` verified RED: 8 failing tests for missing WeClaw command/client implementation

## Validation Results

- RED test state verified for `tests/integration/weclaw-command.test.ts`.

## Blocker Notes

- Gateway contract must be available in `test` before hosted `--env test` smoke can be treated as complete.
